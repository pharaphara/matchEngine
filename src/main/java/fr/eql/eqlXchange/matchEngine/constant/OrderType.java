package fr.eql.eqlXchange.matchEngine.constant;

public enum OrderType {

	/**
	 * Buying order (the trader is providing the counter currency)
	 */
	BID,
	/**
	 * Selling order (the trader is providing the base currency)
	 */
	ASK
}